# LOG DE LECCIONES APRENDIDAS - BUNUELOS_TUREDONDITO

Este documento registra los aprendizajes técnicos, de negocio y de proceso para garantizar la mejora continua del sistema de pronóstico de **Bunuelos SAS**.

---

| ID | Fecha | Fase | Categoría | Hecho / Hallazgo | Por Qué (Causa Raíz) | Acción Accionable |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| LL_001 | 2026-03-13 | 1.1 | PROCESS | Implementación de gobernanza robusta antes del código. | Evitar deriva técnica y falta de trazabilidad en proyectos previos de Forecasting. | Mantener el Mandato del Índice y SDD como pilares innegociables. |
| LL_002 | 2026-03-13 | 1.1 | TECHNICAL | Fallo de CI por carpetas vacías (src). | Git no trackea carpetas sin archivos por defecto, causando fallos en tests de estructura. | Usar `.gitkeep` en todas las carpetas base de la arquitectura (src, data, etc.). |
| LL_003 | 2026-03-13 | 1.1 | TECHNICAL | Integración de Release Please requiere PAT específico. | El token por defecto de GHA no tiene permisos suficientes para disparar otros workflows tras un squash merge. | Configurar `RELEASE_PLEASE_TOKEN` como Secret para habilitar releases automáticos. |
| LL_004 | 2026-03-13 | 1.1 | PROCESS | Restricción de autonomía del agente (Regla C1.4). | El usuario prefiere control total sobre el avance de archivos y fases para evitar cambios no supervisados. | Mantener las reglas C1.3 y C1.4 en `.clinerules` para forzar paradas de validación. |
| LL_005 | 2026-03-14 | 1.2 | TECHNICAL | Singleton Guard Pattern previene inundación de sockets. | Múltiples importaciones en pipelines asíncronos pueden saturar los pools de conexión de Supabase (Max 200 en planes base). | Implementar el Singleton en el método `__new__` con Lazy Loading de variables de entorno. |
| LL_006 | 2026-03-14 | 1.2 | ARCH | Dual-Access Client (Service Role) facilita la observabilidad. | Las políticas RLS restringen la escritura de logs técnicos si no hay un usuario autenticado. | Separar el `Anon Client` del `Service Client` permitiendo escritura de logs de sistema sin vulnerar RLS. |
| LL_007 | 2026-03-14 | 1.2 | PROCESS | Configuración Centralizada (Zero Hardcoding). | El código quemado dificulta la migración entre entornos y la trazabilidad de rutas. | Usar `config.yaml` como Única Fuente de Verdad para todas las rutas y parámetros técnicos. |
| LL_008 | 2026-03-14 | 1.2 | TECHNICAL | Doble Persistencia de Reportes (Latest/History). | Se perdía la trazabilidad histórica de los spikes de rendimiento al sobreescribir archivos. | Implementar guardado dual: archivo `latest` para consumo rápido y `history/` con timestamp para auditoría. |

| LL_010 | 2026-03-14 | 1.3 | TECHNICAL | Degradación de Tipos (Object casting) en APIs REST. | Supabase serializa fechas como String, causando que Pandas las detecte como `object`. | Parametrizar contratos como `datetime` y usar coerción forzada para validar integridad. |
| LL_011 | 2026-03-14 | 1.3 | PROCESS | Contratos de Datos Agnósticos (Gobernanza Pura). | Definir contratos ayuda a desacoplar la base de datos del pipeline, permitiendo fallos controlados. | El contrato debe describir cómo DEBE ser el dato, no cómo llega de la fuente. |
| LL_012 | 2026-03-14 | 1.3 | PROCESS | Suite de Validación Jerárquica [T-1.3-05]. | Modificar el contrato puede romper la sincronización con la configuración global (`config.yaml`). | Implementar pruebas que validen la alineación entre fuente mandatoria y target en el contrato. |
| LL_013 | 2026-03-14 | 1.3 | PROCESS | Sincronización Estricta de DDL SQL. | Columnas desactualizadas en el contrato causan fallos en las queries futuras. | Siempre sincronizar el `.yaml` contra el DDL oficial de Supabase antes de cerrar contratos. |

| LL_014 | 2026-03-14 | 2.1 | TECHNICAL | Detección de Deltas por Hash Semántico. | Evitar procesar datos idénticos ahorra costos de computación y previene duplicados en el modelo. | Implementar el `Semantic Fingerprint` basado en esquema, conteo y muestra de datos (BR-21-05). |
| LL_015 | 2026-03-14 | 2.1 | DATA | Perfilamiento Avanzado para Observabilidad. | Solo validar tipos no es suficiente para IA; detectar atípicos (outliers) previene sesgos en el Forecast. | Incluir Cuartiles, IQR y análisis de frecuencias categóricas en cada reporte de validación. |
| LL_016 | 2026-03-14 | 2.1 | ARCH | Blindaje "Día Cero" (Fail-Safe Architecture). | Si falla la configuración inicial, el sistema debe reportar el error antes de colapsar localmente. | Registrar `GOVERNMENT_ERROR` en Supabase incluso si no existe el archivo de contrato. |
| LL_017 | 2026-03-14 | 2.1 | PROCESS | Alineación Negocio-Carpeta (Stage Load). | Los nombres técnicos (`stage_validator`) pueden confundir al negocio sobre el negocio sobre el avance real del pipeline. | Renombrar etapas de salida basándose en procesos de negocio (Load, Train, Forecast) no en herramientas. |
| LL_018 | 2026-03-15 | 2.2 | TECHNICAL | Ingestión por Lotes (Bypassing Limits). | Supabase restringe a 1000 filas por request, lo cual es insuficiente para series históricas largas. | Implementar descarga iterativa por batches de 1000 hasta agotar el dataset fuente. |
| LL_019 | 2026-03-15 | 2.2 | ARCH | Infraestructura Cloud-Native con DVC y S3. | Los archivos binarios (Parquet) ensucian el historial de Git y se pierden si no hay backup. | Sincronizar la capa Bronce con Supabase Storage (S3) mediante DVC para trazabilidad total. |
| LL_020 | 2026-03-15 | 2.2 | DATA | Evaluación Segura de Reglas de Negocio Complejas. | Las reglas que involucran implicaciones lógicas (=>) o positividad global requieren evaluación dinámica. | Implementar un motor de reglas que use `eval()` con namespace seguro y validación de sintaxis previa. |
| LL_021 | 2026-03-15 | 2.2 | PROCESS | Auditoría de Salud en Ingesta (Drift & Freshness). | Detectar errores de calidad después de la ingesta es costoso para el entrenamiento del modelo. | Integrar análisis de Gaps y Freshness directamente en el proceso de carga hacia la capa Bronce. |
| LL_022 | 2026-03-15 | 2.2 | TECHNICAL | Inicialización de Variables en Flujos Críticos. | El error `UnboundLocalError` bloqueó el Hito 0 porque las listas de control solo se definían en condiciones de éxito. | Siempre inicializar estructuras de control (`failed_tables = []`) antes de bloques de lógica condicional. |
| LL_023 | 2026-03-15 | 2.2 | ARCH | Lógica de Modo Global Inclusiva. | Reportar `NO_NEW_DATA` cuando una tabla mandatoria falla pero otras cargan genera falsos negativos de actividad. | Redefinir el `load_type` global para que refleje la actividad máxima detectada en cualquiera de las fuentes habilitadas. |
| LL_024 | 2026-03-15 | 2.2 | PROCESS | Gatekeeper Evolutivo vs Estático. | Bloquear tablas desconocidas impide el crecimiento orgánico del pipeline definido en el contrato. | El Gatekeeper debe permitir nuevas fuentes por defecto y solo bloquear aquellas con fallos certificados pendientes. |
| LL_025 | 2026-03-15 | 2.2 | TECHNICAL | Reporte de Auditoría como Fuente de Verdad Granular. | Una sola etiqueta global no basta para diagnosticar fallos en pipelines multivariable. | Estructurar el reporte JSON para que cada tabla sea un micro-diagnóstico independiente (estrategia, salud, muestras). |

| LL_026 | 2026-03-16 | 2.2 | TECHNICAL | Aislamiento de Lógica para Testabilidad. | La lógica de cálculo mezclada con componentes de React (UI) es imposible de probar automáticamente. | Extraer motores matemáticos (Drift, Readiness) a archivos `.ts` independientes para habilitar Unit Testing puro. |
| LL_027 | 2026-03-16 | 2.2 | TECHNICAL | Entorno de Pruebas Virtual (JSDOM). | El dashboard depende de objetos del navegador (window, document) que no existen en Node.js puro. | Configurar `vitest` con `jsdom` para simular el navegador y permitir pruebas de renderizado de componentes. |
| LL_028 | 2026-03-16 | 2.2 | PROCESS | Quality Gate Multi-Stack (Python & Node). | Un fallo en el frontend puede ser tan crítico como un fallo en los datos, pero se validaban por separado. | Unificar el CI Quality Gate para que bloquee el merge si fallan tanto la tubería de datos como el dashboard. |
| LL_029 | 2026-03-16 | 2.2 | INFRA | Despliegue Atómico y Continuo (Vercel). | El despliegue manual es propenso a errores de configuración de variables de entorno (Keys de Supabase). | Vincular Vercel a la rama `main` y usar variables de entorno cloud para garantizar despliegues idénticos al entorno local. |
| LL_030 | 2026-03-16 | 2.2 | TECHNICAL | Mocks de Base de Datos para CI. | Las pruebas en GitHub Actions no tienen acceso (ni deben tener) a la base de datos real de Supabase. | Implementar inyección de dependencias y Mocks de Supabase para testear el flujo de datos sin latencia ni dependencias externas. |
| LL_031 | 2026-03-16 | 2.2 | INFRA | Orquestación Autónoma con Time-Zone Offset. | Los cronjobs de GitHub corren en UTC, lo que desfasa la sincronía con la llegada de datos locales (COL). | Configurar el scheduler a las 07:00 UTC para garantizar la ejecución a las 02:00 AM COL, permitiendo una ventana de 1 hora de buffer. |
| LL_032 | 2026-03-16 | 2.2 | ARCH | Sincronización SSoT Cloud-Native (Sin dependencias locales). | Depender de scripts de sincronización manual para actualizar la configuración del dashboard genera deuda técnica y posibles descalces de datos. | Eliminar scripts de sincronización (`sync_dashboard_config.py`) y forzar al Dashboard a leer el contrato certificado directamente desde Supabase (`sys_validation_contract.support_json`). |
| LL_033 | 2026-03-16 | 2.2 | INFRA | Gestión Selectiva de Git para DVC (No-Noise Policy). | Los reportes de ejecución locales (`outputs/`) ensuciaban el historial de Git y causaban fallos en CI al intentar hacer commmits sin cambios en datos. | Ignorar `outputs/` en `.gitignore` y configurar el auto-commit de GHA para que solo actúe si existen cambios reales en los punteros `.dvc`. |
| LL_034 | 2026-03-16 | 2.2 | PROCESS | Gobernanza Dinámica vía Metadata de Carga. | Cambiar el estado de una tabla en el contrato requiere una ejecución de validación para que se refleje en el SSoT de la nube. | El Dashboard debe reflejar el estado autorizado basándose en la metadata del último contrato validado en Supabase, no en archivos estáticos. |
| LL_035 | 2026-03-16 | 2.2 | INFRA | Bootstrap de DVC con Credenciales Locales Silenciosas. | Las credenciales S3 en CI/CD no deben mezclarse con la configuración global de DVC para evitar fugas de secretos y fallos de permisos. | Usar `dvc config --local` para inyectar credenciales temporales en el pipeline sin modificar el archivo `.dvc/config` público. |
| LL_036 | 2026-03-16 | 2.2 | UX | Micro-Observabilidad en Dashboard (Last Sync). | El usuario necesita saber qué tan frescos son los datos sin tener que navegar por logs técnicos. | Implementar un indicador "Last sync" ultra-discreto (9px, 50% opacity) en el header, alimentado directamente por el timestamp del contrato en Supabase. |
| LL_037 | 2026-03-17 | 2.2 | UX | Claridad Semántica en Dimensiones de Calidad. | Confusión entre el nombre de la columna aggregate ("Integrity") y la dimensión técnica de integridad (nulos/tipos). | Renombrar la columna agregada a "Overall Quality" para reflejar que es una media ponderada de los 4 pilares. |
| LL_038 | 2026-03-17 | 2.2 | ARCH | Modo Recuperación vs Aborto Crítico. | Un Gatekeeper estático que aborta si el último contrato es INVALID impide que el sistema se sane solo tras aplicar correcciones. | Implementar un "Recovery Mode" que permita procesar datos incluso tras un fallo previo para validar la recuperación. |
| LL_039 | 2026-03-17 | 2.2 | TECHNICAL | Robustez en Mocks de Comparación Lógica. | Las comparaciones como `MagicMock > MagicMock` (en lógica incremental) disparan `TypeError` en el suite de pruebas. | Definir siempre `return_value` específicos para métodos usados en lógica de control (`_get_remote_count`, etc.) para garantizar caminos de prueba determinísticos. |
| LL_040 | 2026-03-17 | 2.2 | UX | Diferenciación entre Estado y Acción. | La etiqueta "Sync" en la tarjeta de resumen se confundía con un botón interactivo o un proceso en curso. | Actualizar el label a "Last Sync" para denotar telemetría de estado y no una acción pendiente del usuario. |
